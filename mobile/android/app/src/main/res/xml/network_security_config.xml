<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Captive gateway and AWING currently require HTTP. Requests remain bound to a selected Network. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>